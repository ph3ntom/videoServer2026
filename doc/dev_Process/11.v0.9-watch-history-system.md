# v0.9 - 시청 기록 & 이어보기 시스템 구현

## 개요
비디오 시청 기록 및 이어보기 시스템 구현 (Phase 2.3)

**작업 기간**: 2026-01-13
**버전**: v0.9
**주요 기능**: 시청 위치 저장, 이어보기, 진행률 표시

---

## 📊 데이터베이스 스키마

### watch_history 테이블
```sql
CREATE TABLE watch_history (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    video_id INTEGER NOT NULL,
    watch_position INTEGER NOT NULL DEFAULT 0,  -- 현재 재생 위치 (초)
    watch_duration INTEGER NOT NULL DEFAULT 0,  -- 총 시청 시간 (초)
    completed BOOLEAN NOT NULL DEFAULT FALSE,   -- 시청 완료 여부 (>= 90%)
    last_watched_at DATETIME NOT NULL,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (video_id) REFERENCES videos(id) ON DELETE CASCADE,

    CONSTRAINT uq_user_video_watch_history UNIQUE (user_id, video_id)
);

CREATE INDEX ix_watch_history_id ON watch_history(id);
```

### 제약 조건
- **UniqueConstraint**: (user_id, video_id) - 한 사용자는 한 비디오에 하나의 시청 기록만 유지
- **CASCADE**: 사용자나 비디오 삭제 시 관련 시청 기록도 자동 삭제

### 마이그레이션
- **파일**: `backend/alembic/versions/20260113_1008_2f1e7b15dd7f_add_watch_history_table.py`
- **실행**: `alembic upgrade head`

---

## 🔧 백엔드 구현

### 1. 모델 (backend/app/models/watch_history.py)

```python
class WatchHistory(Base):
    """Watch history model for tracking video playback progress"""
    __tablename__ = "watch_history"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    video_id = Column(Integer, ForeignKey("videos.id", ondelete="CASCADE"), nullable=False)
    watch_position = Column(Integer, nullable=False, default=0)
    watch_duration = Column(Integer, nullable=False, default=0)
    completed = Column(Boolean, default=False, nullable=False)
    last_watched_at = Column(DateTime, default=datetime.utcnow, nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)

    # Relationships
    user = relationship("User", backref="watch_history")
    video = relationship("Video", backref="watch_history")

    @property
    def progress_percentage(self) -> float:
        """Calculate watch progress percentage"""
        if not self.video or not self.video.duration or self.video.duration == 0:
            return 0.0
        return min(100.0, (self.watch_position / self.video.duration) * 100.0)
```

**주요 필드**:
- `watch_position`: 마지막 시청 위치 (초 단위)
- `watch_duration`: 총 시청 시간
- `completed`: 90% 이상 시청 시 True
- `progress_percentage`: 계산된 진행률 프로퍼티

### 2. 스키마 (backend/app/schemas/watch_history.py)

```python
class WatchHistoryUpdate(BaseModel):
    """Schema for updating watch history"""
    watch_position: int = Field(..., ge=0)
    watch_duration: int = Field(0, ge=0)

class WatchHistoryResponse(WatchHistoryBase):
    """Schema for watch history response"""
    id: int
    user_id: int
    video_id: int
    watch_duration: int
    completed: bool
    progress_percentage: float
    last_watched_at: datetime
    created_at: datetime
    updated_at: datetime

class ContinueWatchingItem(BaseModel):
    """Schema for continue watching list item"""
    video_id: int
    video_title: str
    video_description: str | None
    video_duration: int | None
    video_thumbnail_path: str | None
    uploader_username: str
    watch_position: int
    progress_percentage: float
    last_watched_at: datetime
```

### 3. 서비스 (backend/app/services/watch_history_service.py)

주요 메서드:
- `save_or_update_watch_history()`: 시청 위치 저장/업데이트 (upsert)
- `get_watch_history()`: 특정 비디오의 시청 기록 조회
- `get_continue_watching_list()`: 이어보기 목록 조회
- `delete_watch_history()`: 시청 기록 삭제

**Upsert 로직**:
```python
# Check if watch history exists
existing = await db.execute(
    select(WatchHistory).where(
        and_(
            WatchHistory.user_id == user_id,
            WatchHistory.video_id == video_id
        )
    )
)

# Calculate completion (>= 90% watched)
if video.duration and video.duration > 0:
    progress = (watch_data.watch_position / video.duration) * 100
    completed = progress >= 90.0

if existing:
    # Update existing record
    existing.watch_position = watch_data.watch_position
    existing.completed = completed
else:
    # Create new record
    new_history = WatchHistory(...)
```

**이어보기 목록 조회**:
```python
result = await db.execute(
    select(WatchHistory)
    .where(
        and_(
            WatchHistory.user_id == user_id,
            WatchHistory.completed == False,  # 완료되지 않은 것만
            WatchHistory.watch_position > 0   # 최소 1초 이상 시청
        )
    )
    .order_by(desc(WatchHistory.last_watched_at))  # 최근 시청 순
    .limit(limit)
)
```

### 4. API 엔드포인트 (backend/app/api/v1/watch_history.py)

| Method | Endpoint | 설명 | 인증 필요 |
|--------|----------|------|----------|
| POST | `/api/v1/videos/{video_id}/watch-history` | 시청 위치 저장/업데이트 | ✅ |
| GET | `/api/v1/videos/{video_id}/watch-history` | 시청 기록 조회 | ✅ |
| GET | `/api/v1/videos/continue-watching` | 이어보기 목록 조회 | ✅ |
| DELETE | `/api/v1/videos/{video_id}/watch-history` | 시청 기록 삭제 | ✅ |

**API 응답 예시**:

```json
// POST /api/v1/videos/12/watch-history
{
  "watch_position": 123,
  "watch_duration": 123
}

// Response
{
  "id": 1,
  "user_id": 1,
  "video_id": 12,
  "watch_position": 123,
  "watch_duration": 123,
  "completed": false,
  "progress_percentage": 45.5,
  "last_watched_at": "2026-01-13T10:00:00",
  "created_at": "2026-01-13T09:50:00",
  "updated_at": "2026-01-13T10:00:00"
}

// GET /api/v1/videos/continue-watching
[
  {
    "video_id": 12,
    "video_title": "4K test",
    "video_description": "test",
    "video_duration": 126,
    "video_thumbnail_path": "/path/to/thumbnail.webp",
    "uploader_username": "admin",
    "watch_position": 57,
    "progress_percentage": 45.2,
    "last_watched_at": "2026-01-13T10:00:00"
  }
]
```

---

## 🎨 프론트엔드 구현

### 1. VideoPlayer 컴포넌트 - 자동 저장

**파일**: `frontend/src/pages/VideoPlayer.tsx`

**구현 기능**:
1. **시청 기록 로드 및 이어보기**
   ```typescript
   player.ready(async () => {
     // Load watch history
     const historyResponse = await apiClient.get(`/videos/${video.id}/watch-history`);
     const watchHistory = historyResponse.data;

     // Resume from last position if not near the end
     if (watchHistory.watch_position > 5 && watchHistory.progress_percentage < 95) {
       player.currentTime(watchHistory.watch_position);
     }
   });
   ```

2. **재생 중 10초마다 자동 저장**
   ```typescript
   player.on('play', () => {
     if (user) {
       saveProgressInterval = setInterval(() => {
         saveWatchProgress();
       }, 10000);
     }
   });
   ```

3. **일시정지 시 저장**
   ```typescript
   player.on('pause', () => {
     if (saveProgressInterval) {
       clearInterval(saveProgressInterval);
     }
     if (user) {
       saveWatchProgress();  // Save one last time
     }
   });
   ```

4. **비디오 종료 시 저장**
   ```typescript
   player.on('ended', () => {
     if (saveProgressInterval) {
       clearInterval(saveProgressInterval);
     }
     if (user) {
       saveWatchProgress();
     }
   });
   ```

5. **저장 함수**
   ```typescript
   const saveWatchProgress = async () => {
     const currentTime = Math.floor(playerRef.current.currentTime());
     const duration = Math.floor(playerRef.current.duration());

     await apiClient.post(`/videos/${video.id}/watch-history`, {
       watch_position: currentTime,
       watch_duration: duration
     });
   };
   ```

### 2. VideoCard 컴포넌트 - 진행률 프로그레스바

**파일**: `frontend/src/components/video/VideoCard.tsx`

**추가된 필드**:
```typescript
interface Video {
  // ... existing fields
  watch_progress?: number; // Progress percentage (0-100)
}
```

**진행률 바 UI**:
```tsx
{/* Watch progress bar */}
{video.watch_progress && video.watch_progress > 0 && (
  <div className="absolute bottom-0 left-0 right-0 h-1 bg-gray-700">
    <div
      className="h-full bg-red-600 transition-all"
      style={{ width: `${video.watch_progress}%` }}
    />
  </div>
)}
```

**스타일**:
- 썸네일 하단에 1px 높이의 빨간색 프로그레스 바
- 진행률에 따라 너비 조절
- YouTube 스타일

### 3. Videos 페이지 - 이어보기 섹션

**파일**: `frontend/src/pages/Videos.tsx`

**추가된 기능**:
```typescript
const [continueWatching, setContinueWatching] = useState<ContinueWatchingItem[]>([]);

const loadContinueWatching = async () => {
  const response = await apiClient.get('/videos/continue-watching?limit=6');
  setContinueWatching(response.data);
};

// Transform to Video format with watch_progress
const continueWatchingVideos: Video[] = continueWatching.map(item => ({
  id: item.video_id,
  title: item.video_title,
  // ...
  watch_progress: item.progress_percentage
}));
```

**UI 배치**:
```tsx
{/* Continue Watching Section */}
{user && continueWatchingVideos.length > 0 && (
  <div className="mb-12">
    <h2 className="text-2xl font-bold text-white mb-4">이어보기</h2>
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
      {continueWatchingVideos.map((video) => (
        <VideoCard key={video.id} video={video} />
      ))}
    </div>
  </div>
)}
```

**조건**:
- 로그인 사용자만 표시
- 시청 기록이 있는 경우만 표시
- 최대 6개 표시

---

## 🧪 테스트 시나리오

### 1. 시청 위치 저장
1. ✅ 비디오 재생 시작
2. ✅ 10초마다 자동 저장
3. ✅ 일시정지 시 즉시 저장
4. ✅ 비디오 종료 시 저장

### 2. 이어보기
1. ✅ 비디오를 중간까지 시청
2. ✅ 페이지 새로고침
3. ✅ 마지막 시청 위치부터 재생
4. ✅ 95% 이상 시청 시 처음부터 재생

### 3. 이어보기 목록
1. ✅ 여러 비디오를 일부만 시청
2. ✅ 홈 페이지에서 이어보기 섹션 확인
3. ✅ 최근 시청 순으로 정렬됨
4. ✅ 진행률 프로그레스 바 표시

### 4. 시청 완료
1. ✅ 비디오를 90% 이상 시청
2. ✅ `completed = true`로 저장
3. ✅ 이어보기 목록에서 제외됨

---

## 📈 성능 최적화

### 데이터베이스
- `watch_history.id`에 인덱스 생성
- `(user_id, video_id)` 유니크 제약 조건 활용
- JOIN 최소화 (`selectinload` 사용)

### API
- 시청 위치 저장: 10초 간격으로 throttle
- 이어보기 목록: 최대 10개로 제한
- 불필요한 API 호출 최소화 (조건부 로드)

### 프론트엔드
- Hot Module Replacement (HMR) 지원
- setInterval 정리 (메모리 누수 방지)
- Video.js 이벤트 리스너 최적화

---

## 🔄 데이터 흐름

### 시청 위치 저장
```
[사용자 비디오 시청]
  ↓ 10초마다
[VideoPlayer.saveWatchProgress]
  ↓ POST /api/v1/videos/12/watch-history
    {watch_position: 57, watch_duration: 126}
[watch_history API]
  ↓ watch_history_service.save_or_update_watch_history()
[PostgreSQL watch_history 테이블]
  ↓ UPSERT (INSERT or UPDATE)
  ↓ completed 계산 (>= 90%)
[WatchHistoryResponse]
  ↓ 200 OK
[저장 완료]
```

### 이어보기
```
[비디오 페이지 로드]
  ↓ player.ready()
  ↓ GET /api/v1/videos/12/watch-history
[시청 기록 조회]
  ↓ {watch_position: 57, progress_percentage: 45.2}
[VideoPlayer]
  ↓ player.currentTime(57)
[57초 위치부터 재생 시작]
```

### 이어보기 목록
```
[홈 페이지 로드]
  ↓ if (user)
  ↓ GET /api/v1/videos/continue-watching?limit=6
[watch_history_service]
  ↓ SELECT * FROM watch_history
    WHERE completed = false AND watch_position > 0
    ORDER BY last_watched_at DESC
    LIMIT 6
[ContinueWatchingItem[]]
  ↓ Transform to Video[]
[이어보기 섹션 렌더링]
  ↓ VideoCard (with progress bar)
```

---

## 📝 향후 개선 사항

### 기능 추가
- [ ] 시청 기록 삭제 버튼 (UI)
- [ ] 시청 완료 비디오 목록
- [ ] 시청 통계 (총 시청 시간, 완료한 비디오 수)
- [ ] 비디오 추천 (시청 기록 기반)

### 성능 최적화
- [ ] Redis 캐싱 (이어보기 목록)
- [ ] 배치 저장 (여러 요청 합치기)
- [ ] WebSocket 사용 (실시간 진행률 동기화)

---

## 🐛 트러블슈팅

### 1. setInterval 메모리 누수
**원인**: clearInterval 누락
**해결**: useEffect cleanup에서 clearInterval 호출

### 2. 시청 위치가 0초로 리셋됨
**원인**: player.currentTime() 호출 타이밍 문제
**해결**: player.ready() 콜백에서 currentTime 설정

### 3. 이어보기 목록이 업데이트되지 않음
**원인**: user 상태 변경 감지 안됨
**해결**: useEffect 의존성 배열에 user 추가

---

## 📊 코드 통계

### 백엔드
- **파일 추가**: 3개
  - `models/watch_history.py` (50 lines)
  - `schemas/watch_history.py` (60 lines)
  - `services/watch_history_service.py` (200 lines)
  - `api/v1/watch_history.py` (145 lines)
- **파일 수정**: 2개
  - `models/__init__.py` (2 lines)
  - `api/v1/router.py` (2 lines)
- **마이그레이션**: 1개 (47 lines)

### 프론트엔드
- **파일 수정**: 3개
  - `pages/VideoPlayer.tsx` (+115 lines)
  - `components/video/VideoCard.tsx` (+10 lines)
  - `pages/Videos.tsx` (+50 lines)

### 총계
- **새로운 코드**: ~504 lines
- **수정된 코드**: ~179 lines
- **작업 시간**: 약 1시간

---

## ✅ 체크리스트

- [x] watch_history 테이블 마이그레이션 생성 및 적용
- [x] WatchHistory 모델 정의 (제약 조건, 프로퍼티)
- [x] WatchHistory Schema 정의
- [x] WatchHistory Service 구현 (CRUD, 이어보기 목록)
- [x] Watch History API 엔드포인트 구현
- [x] API 라우터 등록
- [x] VideoPlayer 시청 위치 자동 저장 로직
- [x] VideoPlayer 이어보기 기능
- [x] VideoCard 진행률 프로그레스 바
- [x] Videos 페이지 이어보기 섹션
- [x] 백엔드 API 테스트
- [x] 프론트엔드 동작 확인
- [x] 문서화

---

## 📚 참고 자료

- [YouTube Resume Playback](https://support.google.com/youtube/answer/9012523)
- [Video.js Events](https://videojs.com/guides/events/)
- [SQLAlchemy Relationships](https://docs.sqlalchemy.org/en/20/orm/relationships.html)

---

**작성일**: 2026-01-13
**작성자**: Claude Sonnet 4.5
**버전**: v0.9
