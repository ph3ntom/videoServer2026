# Phase 2.7: 정렬 및 페이지네이션 개선

**날짜**: 2026-01-15
**버전**: Phase 2.7
**상태**: ✅ 완료

---

## 📋 개요

메인 페이지의 각 섹션을 간결하게 유지하면서, 전용 페이지에서 더 많은 콘텐츠를 탐색할 수 있도록 페이지네이션 시스템을 개선했습니다.

---

## 🎯 사용자 요구사항

1. **메인 페이지**: 각 섹션(인기 동영상, 높은 평점, 모든 비디오)에 4개씩만 표시하고 "더보기" 링크 추가
2. **인기 동영상 페이지**: 4x5 그리드(20개/페이지), 5페이지 분량 (최대 100개)
3. **높은 평점 페이지**: 4x5 그리드(20개/페이지), 5페이지 분량 (최대 100개)
4. **모든 비디오 페이지**: 4x5 그리드(20개/페이지), 동영상 개수에 따른 동적 페이지네이션

---

## 🔧 구현 내역

### 1. 백엔드 개선

#### 1.1 Schema 추가 (`backend/app/schemas/video.py`)

**새로운 Schema**: `VideoListPaginatedResponse`

```python
class VideoListPaginatedResponse(BaseModel):
    """Schema for paginated video list response"""
    items: List[VideoListResponse]
    total: int
    page: int
    page_size: int
    total_pages: int

    model_config = ConfigDict(from_attributes=True)
```

**목적**: 페이지네이션 정보와 함께 동영상 목록 반환

---

#### 1.2 Video Service 개선 (`backend/app/services/video_service.py`)

**새로운 메서드**: `count_all`

```python
async def count_all(
    db: AsyncSession,
    status: Optional[VideoStatus] = VideoStatus.READY,
    tag_ids: Optional[List[int]] = None
) -> int:
    """Count all videos with optional tag filtering"""
    from app.models.associations import video_tags

    query = select(func.count(Video.id))

    if status:
        query = query.where(Video.status == status)

    # Filter by tags if provided
    if tag_ids:
        query = (
            query
            .join(video_tags, Video.id == video_tags.c.video_id)
            .where(video_tags.c.tag_id.in_(tag_ids))
            .distinct()
        )

    result = await db.execute(query)
    return result.scalar() or 0
```

**기능**:
- 전체 동영상 개수 조회
- 태그 필터링 지원
- 동적 페이지네이션을 위한 total count 제공

---

#### 1.3 Videos API 개선 (`backend/app/api/v1/videos.py`)

**새로운 엔드포인트**: `GET /videos/paginated`

```python
@router.get("/paginated", response_model=VideoListPaginatedResponse)
async def list_videos_paginated(
    page: int = Query(1, ge=1, description="Page number (starting from 1)"),
    page_size: int = Query(20, ge=1, le=100, description="Number of videos per page"),
    tag_ids: str = Query(None, description="Comma-separated tag IDs to filter by"),
    db: AsyncSession = Depends(get_db)
):
    # Calculate skip value
    skip = (page - 1) * page_size

    # Get total count first
    total = await video_service.count_all(
        db,
        status=VideoStatus.READY,
        tag_ids=tag_id_list
    )

    # Then get videos
    videos = await video_service.get_all(
        db,
        skip=skip,
        limit=page_size,
        status=VideoStatus.READY,
        tag_ids=tag_id_list
    )

    # Calculate total pages
    import math
    total_pages = math.ceil(total / page_size) if total > 0 else 0

    return VideoListPaginatedResponse(
        items=items,
        total=total,
        page=page,
        page_size=page_size,
        total_pages=total_pages
    )
```

**주요 특징**:
- page 기반 페이지네이션 (1부터 시작)
- total, total_pages 정보 포함
- 태그 필터링 지원

**중요 버그 수정**:
- 초기 구현에서 `asyncio.gather()`를 사용하여 동시 쿼리 실행
- SQLAlchemy 에러 발생: `This session is provisioning a new connection; concurrent operations are not permitted`
- **해결**: 순차적 실행으로 변경 (count → get_all)

---

#### 1.4 Statistics API 개선 (`backend/app/api/v1/statistics.py`)

**skip 파라미터 추가**:

```python
@router.get("/popular", response_model=List[VideoListResponse])
async def get_popular_videos(
    limit: int = Query(default=10, ge=1, le=100, description="Number of videos to return"),
    skip: int = Query(default=0, ge=0, description="Number of videos to skip for pagination"),
    db: AsyncSession = Depends(get_db)
):
    videos = await statistics_service.get_popular_videos(db, limit=limit, skip=skip)
    # ...

@router.get("/top-rated", response_model=List[TopRatedVideoResponse])
async def get_top_rated_videos(
    limit: int = Query(default=10, ge=1, le=100, description="Number of videos to return"),
    skip: int = Query(default=0, ge=0, description="Number of videos to skip for pagination"),
    min_ratings: int = Query(default=5, ge=1, le=100, description="Minimum number of ratings required"),
    db: AsyncSession = Depends(get_db)
):
    top_rated = await statistics_service.get_top_rated_videos(
        db,
        limit=limit,
        skip=skip,
        min_ratings=min_ratings
    )
    # ...
```

**변경 사항**:
- limit 최대값: 50 → 100
- skip 파라미터 추가

---

#### 1.5 Statistics Service 개선 (`backend/app/services/statistics_service.py`)

**skip 파라미터 추가**:

```python
async def get_popular_videos(
    db: AsyncSession,
    limit: int = 10,
    skip: int = 0
) -> List[Video]:
    query = (
        select(Video)
        .options(joinedload(Video.uploader))
        .where(Video.status == "ready")
        .order_by(desc(Video.view_count))
        .offset(skip)
        .limit(limit)
    )
    # ...

async def get_top_rated_videos(
    db: AsyncSession,
    limit: int = 10,
    skip: int = 0,
    min_ratings: int = 5
) -> List[dict]:
    query = (
        select(...)
        .order_by(desc(rating_stats.c.avg_rating))
        .offset(skip)
        .limit(limit)
    )
    # ...
```

**추가된 기능**:
- `.offset(skip)` 추가로 페이지네이션 지원

---

### 2. 프론트엔드 구현

#### 2.1 PopularVideos.tsx (인기 동영상 전용 페이지)

```typescript
const ITEMS_PER_PAGE = 20;
const TOTAL_PAGES = 5;

export default function PopularVideos() {
  const [videos, setVideos] = useState<Video[]>([]);
  const [currentPage, setCurrentPage] = useState(1);

  const loadPopularVideos = async () => {
    const skip = (currentPage - 1) * ITEMS_PER_PAGE;
    const response = await apiClient.get(
      `/statistics/popular?limit=${ITEMS_PER_PAGE}&skip=${skip}`
    );
    setVideos(response.data);
  };

  // 4x5 그리드
  // 페이지네이션: [이전] [1] [2] [3] [4] [5] [다음]
}
```

**특징**:
- 고정 5페이지 페이지네이션
- 조회수 기준 정렬
- "뒤로 가기" 링크

---

#### 2.2 TopRatedVideos.tsx (높은 평점 전용 페이지)

```typescript
const ITEMS_PER_PAGE = 20;
const TOTAL_PAGES = 5;

export default function TopRatedVideos() {
  const [topRatedVideos, setTopRatedVideos] = useState<TopRatedVideo[]>([]);
  const [currentPage, setCurrentPage] = useState(1);

  const loadTopRatedVideos = async () => {
    const skip = (currentPage - 1) * ITEMS_PER_PAGE;
    const response = await apiClient.get(
      `/statistics/top-rated?limit=${ITEMS_PER_PAGE}&skip=${skip}&min_ratings=1`
    );
    setTopRatedVideos(response.data);
  };

  // 4x5 그리드 + 평점 배지
  // 페이지네이션: [이전] [1] [2] [3] [4] [5] [다음]
}
```

**특징**:
- 고정 5페이지 페이지네이션
- 평균 평점 기준 정렬
- 평점 배지 (★ 4.5 (3)) 표시

---

#### 2.3 AllVideos.tsx (모든 비디오 전용 페이지) - 신규

```typescript
const ITEMS_PER_PAGE = 20;

export default function AllVideos() {
  const [videos, setVideos] = useState<Video[]>([]);
  const [currentPage, setCurrentPage] = useState(1);
  const [totalPages, setTotalPages] = useState(1);
  const [total, setTotal] = useState(0);

  const loadVideos = async () => {
    const response = await apiClient.get<PaginatedResponse>(
      `/videos/paginated?page=${currentPage}&page_size=${ITEMS_PER_PAGE}`
    );
    setVideos(response.data.items);
    setTotal(response.data.total);
    setTotalPages(response.data.total_pages);
  };

  // 스마트 페이지 번호 표시
  const getPageNumbers = () => {
    if (totalPages <= 7) {
      // 전체 페이지 표시
      return [1, 2, 3, 4, 5, 6, 7];
    } else {
      // 1 ... 3 4 5 ... 10 형태
      const pages = [1];
      if (currentPage > 3) pages.push('...');

      const start = Math.max(2, currentPage - 1);
      const end = Math.min(totalPages - 1, currentPage + 1);
      for (let i = start; i <= end; i++) pages.push(i);

      if (currentPage < totalPages - 2) pages.push('...');
      pages.push(totalPages);

      return pages;
    }
  };
}
```

**특징**:
- **동적 페이지네이션** (동영상 개수 기반)
- 스마트 페이지 번호 표시 (최대 7개 버튼)
- 전체 동영상 개수 표시
- "페이지 X / Y (Z개 비디오)" 정보 표시

---

#### 2.4 Videos.tsx (메인 페이지) 수정

**변경 사항**:

```typescript
// limit 8 → 4
const loadPopularVideos = async () => {
  const response = await apiClient.get('/statistics/popular?limit=4');
  setPopularVideos(response.data);
};

const loadTopRatedVideos = async () => {
  const response = await apiClient.get('/statistics/top-rated?limit=4&min_ratings=1');
  setTopRatedVideos(response.data);
};

const loadVideos = async () => {
  const params = selectedTag ? `?tag_ids=${selectedTag}&limit=4` : '?limit=4';
  const response = await apiClient.get(`/videos/${params}`);
  setVideos(response.data);
};
```

**UI 개선**:

```tsx
{/* 인기 동영상 */}
<div className="flex items-center justify-between mb-4">
  <div className="flex items-center gap-2">
    <h2 className="text-2xl font-bold text-white">인기 비디오</h2>
    <span className="text-sm text-gray-400">조회수 기준</span>
  </div>
  <Link to="/videos/popular" className="text-indigo-400 hover:text-indigo-300 text-sm">
    더보기 →
  </Link>
</div>

{/* 높은 평점 */}
<div className="flex items-center justify-between mb-4">
  <div className="flex items-center gap-2">
    <h2 className="text-2xl font-bold text-white">높은 평점</h2>
    <span className="text-sm text-gray-400">평균 평점 기준</span>
  </div>
  <Link to="/videos/top-rated" className="text-indigo-400 hover:text-indigo-300 text-sm">
    더보기 →
  </Link>
</div>

{/* 모든 비디오 */}
<div className="flex items-center justify-between mb-6">
  <h2 className="text-2xl font-bold text-white">모든 비디오</h2>
  {!selectedTag && (
    <Link to="/videos/all" className="text-indigo-400 hover:text-indigo-300 text-sm">
      더보기 →
    </Link>
  )}
</div>
```

---

#### 2.5 App.tsx 라우트 추가

```tsx
import PopularVideos from './pages/PopularVideos'
import TopRatedVideos from './pages/TopRatedVideos'
import AllVideos from './pages/AllVideos'

// ...

<Route path="/videos/popular" element={<PrivateRoute><PopularVideos /></PrivateRoute>} />
<Route path="/videos/top-rated" element={<PrivateRoute><TopRatedVideos /></PrivateRoute>} />
<Route path="/videos/all" element={<PrivateRoute><AllVideos /></PrivateRoute>} />
```

---

## 🐛 버그 및 해결

### 버그 1: SQLAlchemy 동시 작업 에러

**에러 메시지**:
```
sqlalchemy.exc.InvalidRequestError: This session is provisioning a new connection;
concurrent operations are not permitted
```

**원인**:
- `videos.py`의 `list_videos_paginated`에서 `asyncio.gather()` 사용
- 동일한 DB 세션으로 두 개의 쿼리 동시 실행

**문제 코드**:
```python
# 에러 발생
videos, total = await asyncio.gather(
    video_service.get_all(db, ...),
    video_service.count_all(db, ...)
)
```

**해결 방법**:
```python
# 순차 실행으로 수정
total = await video_service.count_all(db, ...)
videos = await video_service.get_all(db, ...)
```

**교훈**:
- SQLAlchemy async session은 동시 쿼리를 지원하지 않음
- 성능보다 안정성을 우선하여 순차 실행

---

## 📁 수정된 파일 목록

### 백엔드 (6개)
1. `backend/app/schemas/video.py` - VideoListPaginatedResponse 추가
2. `backend/app/services/video_service.py` - count_all 메서드 추가
3. `backend/app/api/v1/videos.py` - GET /videos/paginated 엔드포인트 추가
4. `backend/app/api/v1/statistics.py` - skip 파라미터 추가, limit 최대값 100
5. `backend/app/services/statistics_service.py` - skip 파라미터 추가

### 프론트엔드 (5개)
1. `frontend/src/pages/PopularVideos.tsx` - 인기 동영상 전용 페이지 (신규)
2. `frontend/src/pages/TopRatedVideos.tsx` - 높은 평점 전용 페이지 (신규)
3. `frontend/src/pages/AllVideos.tsx` - 모든 비디오 전용 페이지 (신규)
4. `frontend/src/pages/Videos.tsx` - limit 4, 더보기 링크 추가
5. `frontend/src/App.tsx` - 3개 라우트 추가

---

## 🧪 테스트 결과

### 1. 메인 페이지 (/videos)
- [x] 인기 동영상 4개 표시
- [x] 높은 평점 4개 표시
- [x] 모든 비디오 4개 표시
- [x] 각 섹션 "더보기 →" 링크 표시

### 2. 인기 동영상 페이지 (/videos/popular)
- [x] 4x5 그리드 (20개) 표시
- [x] 페이지네이션 [1] [2] [3] [4] [5]
- [x] 조회수 내림차순 정렬
- [x] "뒤로 가기" 링크 작동

### 3. 높은 평점 페이지 (/videos/top-rated)
- [x] 4x5 그리드 (20개) 표시
- [x] 페이지네이션 [1] [2] [3] [4] [5]
- [x] 평점 배지 표시 (★ 4.5 (3))
- [x] 평균 평점 내림차순 정렬

### 4. 모든 비디오 페이지 (/videos/all)
- [x] 4x5 그리드 (20개) 표시
- [x] 동적 페이지네이션 (실제 동영상 개수 기반)
- [x] 전체 동영상 개수 표시 ("전체 3개의 비디오")
- [x] 스마트 페이지 번호 표시
- [x] "페이지 1 / 1 (3개 비디오)" 표시

### 5. 반응형
- [x] 모바일 (< 768px): 1열
- [x] 태블릿 (768px ~ 1024px): 2열
- [x] 데스크톱 (1024px ~ 1280px): 3열
- [x] 대형 화면 (> 1280px): 4열

### 6. 페이지 전환
- [x] 페이지 변경 시 상단으로 스크롤
- [x] 이전/다음 버튼 비활성화 처리
- [x] 현재 페이지 하이라이트

---

## 💡 개선 포인트

### 1. 사용자 경험 (UX)
- 메인 페이지 간결화 (각 섹션 4개)
- 전용 페이지에서 더 많은 콘텐츠 탐색
- 직관적인 "더보기" 네비게이션

### 2. 페이지네이션
- **고정 페이지네이션** (인기/평점): 빠른 로딩, 일관된 UX
- **동적 페이지네이션** (모든 비디오): 실제 데이터 반영, 확장 가능
- 스마트 페이지 번호 표시로 UI 깔끔하게 유지

### 3. 성능
- skip/offset 기반 페이지네이션
- total count 캐싱 가능성 (향후 개선)
- 클라이언트 사이드 페이지 전환 (SPA)

---

## 🔜 향후 개선 사항

### 1. 정렬 옵션 추가
- 모든 비디오: 최신순, 조회수순, 평점순 선택
- URL 쿼리 파라미터로 정렬 상태 유지

### 2. 무한 스크롤
- 모바일 환경에서 무한 스크롤 옵션
- Intersection Observer API 활용

### 3. 성능 최적화
- Total count 캐싱 (Redis)
- Cursor 기반 페이지네이션 검토
- 프리페칭 (다음 페이지 미리 로드)

### 4. 검색 결과 페이지네이션
- 검색 결과도 동일한 페이지네이션 적용
- 검색 필터와 정렬 조합

---

## ✅ 완료 확인

- [x] 메인 페이지 각 섹션 4개로 축소
- [x] 인기 동영상 전용 페이지 구현 (5페이지)
- [x] 높은 평점 전용 페이지 구현 (5페이지)
- [x] 모든 비디오 전용 페이지 구현 (동적 페이지네이션)
- [x] 백엔드 API skip 파라미터 추가
- [x] 백엔드 paginated 엔드포인트 구현
- [x] SQLAlchemy 동시 작업 에러 수정
- [x] 브라우저 테스트 완료
- [x] 반응형 확인 완료
- [x] 사용자 확인: "완벽하게 동작한다"

---

**작성일**: 2026-01-15
**작성자**: Claude Sonnet 4.5
**버전**: Phase 2.7
**상태**: ✅ 완료 및 테스트 완료
