# v0.6 - 비디오 검색 시스템 (Video Search System)

**버전:** v0.6
**개발 기간:** 2026-01-12
**상태:** ✅ 완료

---

## 📋 목차

1. [개요](#개요)
2. [백엔드 구현](#백엔드-구현)
3. [프론트엔드 구현](#프론트엔드-구현)
4. [주요 기능](#주요-기능)
5. [테스트](#테스트)
6. [향후 개선사항](#향후-개선사항)

---

## 개요

### 목적
사용자가 비디오를 빠르고 정확하게 찾을 수 있도록 검색 기능을 제공합니다.

### 구현 범위 (Phase 1 MVP)
06_개발_로드맵.md의 Phase 1 검색 페이지 명세에 따라 구현:

- ✅ **제목 키워드 검색 (1순위)**
  - 검색바 (Header에 통합)
  - Debouncing (300ms)
  - 검색 결과 그리드

- ✅ **태그 조합 검색 (2순위)**
  - 태그 목록 표시
  - 체크박스 + 연산자 선택 UI (포함/제외)
  - 선택된 태그 요약 표시
  - 필터 초기화 버튼

- ✅ **고급 검색 결과 페이지**
  - API 호출 (include_tags, exclude_tags)
  - 검색 결과 그리드 (YouTube 스타일)
  - 적용된 필터 표시

### 검색 범위
1. **비디오 제목** (ILIKE)
2. **비디오 설명** (ILIKE)
3. **업로더 이름** (ILIKE)
4. **태그 필터링** (include/exclude)

### 기술 스택
- **Backend:** FastAPI, SQLAlchemy 2.0 (Async), PostgreSQL ILIKE
- **Frontend:** React 18, TypeScript, TailwindCSS, React Router

---

## 백엔드 구현

### 1. 검색 서비스 (video_service.py)

**파일:** `backend/app/services/video_service.py`

#### search 메서드

```python
@staticmethod
async def search(
    db: AsyncSession,
    query_text: str,
    skip: int = 0,
    limit: int = 20,
    sort_by: str = "created_at",
    include_tags: Optional[List[int]] = None,
    exclude_tags: Optional[List[int]] = None,
    status: Optional[VideoStatus] = VideoStatus.READY
) -> List[Video]:
```

**주요 기능:**
- **키워드 검색:** 제목, 설명, 업로더 이름에서 ILIKE 패턴 매칭
- **include_tags:** 지정된 태그 중 하나라도 가진 비디오 (OR 조건)
- **exclude_tags:** 지정된 태그를 가지지 않은 비디오 (NOT EXISTS)
- **정렬:** created_at (기본), view_count, title
- **페이지네이션:** skip, limit

**SQL 쿼리 로직:**

```python
# 키워드 검색 (ILIKE)
search_filter = or_(
    Video.title.ilike(f"%{query_text}%"),
    Video.description.ilike(f"%{query_text}%"),
    Video.uploader.has(User.username.ilike(f"%{query_text}%"))
)

# Include tags (OR 조건)
if include_tags:
    query = (
        query
        .join(video_tags, Video.id == video_tags.c.video_id)
        .where(video_tags.c.tag_id.in_(include_tags))
        .distinct()
    )

# Exclude tags (NOT EXISTS)
if exclude_tags:
    exclude_subquery = (
        select(video_tags.c.video_id)
        .where(
            and_(
                video_tags.c.video_id == Video.id,
                video_tags.c.tag_id.in_(exclude_tags)
            )
        )
    )
    query = query.where(~exists(exclude_subquery))
```

### 2. 검색 API 엔드포인트 (videos.py)

**파일:** `backend/app/api/v1/videos.py`

**엔드포인트:** `GET /api/v1/videos/search`

**Query Parameters:**

| 파라미터 | 타입 | 필수 | 기본값 | 설명 |
|---------|------|------|--------|------|
| `q` | string | ✅ | - | 검색어 (최소 1자) |
| `skip` | integer | ❌ | 0 | 페이지네이션 오프셋 |
| `limit` | integer | ❌ | 20 | 결과 개수 (최대 100) |
| `sort_by` | string | ❌ | created_at | 정렬 기준 (created_at, view_count, title) |
| `include_tags` | string | ❌ | - | 포함할 태그 ID (쉼표로 구분, 예: "1,3,5") |
| `exclude_tags` | string | ❌ | - | 제외할 태그 ID (쉼표로 구분, 예: "2,4") |

**응답:** `List[VideoListResponse]`

**예제:**

```bash
# 기본 검색
GET /api/v1/videos/search?q=dance

# 정렬 옵션
GET /api/v1/videos/search?q=k-pop&sort_by=view_count

# 태그 포함
GET /api/v1/videos/search?q=직캠&include_tags=1,3

# 태그 포함 + 제외
GET /api/v1/videos/search?q=music&include_tags=1&exclude_tags=5,6
```

---

## 프론트엔드 구현

### 1. SearchBar 컴포넌트

**파일:** `frontend/src/components/search/SearchBar.tsx`

**위치:** Header (Layout.tsx에 통합)

**주요 기능:**
- ✅ Debouncing (300ms) - 입력 후 300ms 대기
- ✅ Enter 키 즉시 검색
- ✅ URL 쿼리 파라미터 동기화
- ✅ 검색 아이콘 버튼

**기술 구현:**

```typescript
// Debouncing with useRef
const timeoutRef = useRef<NodeJS.Timeout | null>(null);

const handleSearch = useCallback((searchQuery: string) => {
  if (timeoutRef.current) {
    clearTimeout(timeoutRef.current);
  }

  timeoutRef.current = setTimeout(() => {
    if (searchQuery.trim().length > 0) {
      navigate(`/search?q=${encodeURIComponent(searchQuery.trim())}`);
    }
  }, 300);
}, [navigate]);

// Enter 키로 즉시 검색
const handleKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
  if (e.key === 'Enter') {
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
    // 즉시 검색
  }
};
```

### 2. SearchPage 컴포넌트

**파일:** `frontend/src/pages/SearchPage.tsx`

**라우트:** `/search?q={query}&sort_by={sort}`

#### 레이아웃 구조

```
┌────────────────────────────────────────┐
│  Header (검색바 포함)                    │
├────────────────────────────────────────┤
│  검색 결과: "{query}" (N개)              │
│  ┌──────────┬─────────────────────────┐│
│  │ 사이드바  │  비디오 그리드             ││
│  │          │  ┌────┬────┬────┐       ││
│  │ 정렬     │  │ V1 │ V2 │ V3 │       ││
│  │ ○ 최신순│  ├────┼────┼────┤       ││
│  │ ○ 인기순│  │ V4 │ V5 │ V6 │       ││
│  │ ○ 제목순│  └────┴────┴────┘       ││
│  │          │                         ││
│  │ 태그필터 │  [더보기]                ││
│  │ [K-pop]  │                         ││
│  │ [−]      │                         ││
│  │          │                         ││
│  │ 적용필터 │                         ││
│  │ 포함: K  │                         ││
│  │ 제외:    │                         ││
│  └──────────┴─────────────────────────┘│
└────────────────────────────────────────┘
```

#### 주요 기능

**1. 정렬 옵션**
- 최신순 (created_at DESC)
- 인기순 (view_count DESC)
- 제목순 (title ASC)

**2. 태그 필터**

```typescript
// 태그 상태: 'include' | 'exclude' | 'none'
const [includeTags, setIncludeTags] = useState<number[]>([]);
const [excludeTags, setExcludeTags] = useState<number[]>([]);

// 포함 토글
const handleTagInclude = (tagId: number) => {
  if (includeTags.includes(tagId)) {
    setIncludeTags(includeTags.filter(id => id !== tagId));
  } else {
    setIncludeTags([...includeTags, tagId]);
    // exclude에서 제거
    setExcludeTags(excludeTags.filter(id => id !== tagId));
  }
};

// 제외 토글
const handleTagExclude = (tagId: number) => {
  // 유사한 로직
};
```

**3. URL 상태 관리**

```typescript
// URL에서 쿼리 파라미터 가져오기
const [searchParams, setSearchParams] = useSearchParams();
const query = searchParams.get('q') || '';
const sortBy = searchParams.get('sort_by') || 'created_at';

// 정렬 변경 시 URL 업데이트
const handleSortChange = (newSort: SortOption) => {
  setSearchParams({ q: query, sort_by: newSort });
};
```

**4. 적용된 필터 요약 표시**

```tsx
{includeTags.length > 0 && (
  <div>
    <span className="text-xs text-green-400">포함: </span>
    <div className="flex flex-wrap gap-1 mt-1">
      {includeTags.map(tagId => {
        const tag = tags.find(t => t.id === tagId);
        return (
          <span style={{ backgroundColor: tag.color }}>
            {tag.name}
          </span>
        );
      })}
    </div>
  </div>
)}
```

### 3. Layout 통합

**파일:** `frontend/src/components/common/Layout.tsx`

**변경사항:**
- SearchBar import 추가
- Header에 SearchBar 컴포넌트 배치 (로고와 네비게이션 사이)
- 로그인한 사용자만 검색바 표시

```tsx
<div className="flex justify-between items-center h-16 gap-4">
  <Link to="/" className="flex items-center flex-shrink-0">
    <span className="text-2xl font-bold text-white">StreamFlix</span>
  </Link>

  {user && <SearchBar className="flex-1 max-w-2xl" />}

  <nav className="flex items-center space-x-4 flex-shrink-0">
    {/* Navigation items */}
  </nav>
</div>
```

### 4. 라우팅 설정

**파일:** `frontend/src/App.tsx`

```tsx
import SearchPage from './pages/SearchPage'

<Route
  path="/search"
  element={
    <PrivateRoute>
      <SearchPage />
    </PrivateRoute>
  }
/>
```

---

## 주요 기능

### 1. 기본 키워드 검색

**사용 흐름:**
1. 사용자가 헤더 검색바에 "dance" 입력
2. 300ms Debouncing 후 `/search?q=dance`로 리다이렉트
3. SearchPage에서 URL 파라미터 읽기
4. API 호출: `GET /api/v1/videos/search?q=dance`
5. 제목, 설명, 업로더 이름에 "dance"가 포함된 비디오 반환
6. 검색 결과 그리드에 표시

### 2. 태그 조합 검색

**포함 필터 (include_tags):**
- 사용자가 "K-pop", "Dance" 태그 선택
- API: `/api/v1/videos/search?q=music&include_tags=1,3`
- 비디오가 태그 1 **또는** 태그 3을 가지고 있어야 함 (OR 조건)

**제외 필터 (exclude_tags):**
- 사용자가 "Cover" 태그를 제외로 선택
- API: `/api/v1/videos/search?q=music&exclude_tags=5`
- 태그 5를 가진 비디오는 결과에서 제외

**조합 사용:**
```
/api/v1/videos/search?q=concert&include_tags=1,2&exclude_tags=5,6
```
- 제목에 "concert" 포함
- AND 태그 1 또는 2를 가짐
- AND 태그 5나 6를 가지지 않음

### 3. 정렬 옵션

**최신순 (기본):**
```sql
ORDER BY created_at DESC
```

**인기순:**
```sql
ORDER BY view_count DESC
```

**제목순:**
```sql
ORDER BY title ASC
```

### 4. 검색 결과 표시

- **비디오 그리드:** 3열 그리드 레이아웃 (반응형)
- **VideoCard 컴포넌트 재사용:** 썸네일 호버 프리뷰 포함
- **빈 결과 처리:** "검색 결과가 없습니다" 메시지

---

## 테스트

### 백엔드 API 테스트

```bash
# 1. 기본 검색
curl "http://localhost:8000/api/v1/videos/search?q=test"

# 2. 정렬 (인기순)
curl "http://localhost:8000/api/v1/videos/search?q=test&sort_by=view_count"

# 3. include_tags
curl "http://localhost:8000/api/v1/videos/search?q=test&include_tags=1,2"

# 4. exclude_tags
curl "http://localhost:8000/api/v1/videos/search?q=test&exclude_tags=1,2"

# 5. 조합
curl "http://localhost:8000/api/v1/videos/search?q=music&include_tags=1&exclude_tags=5"
```

### 프론트엔드 테스트

**테스트 시나리오:**

1. **검색바 입력**
   - 검색어 입력 → 300ms 후 검색 페이지 이동
   - Enter 키 → 즉시 검색

2. **정렬 변경**
   - 최신순 클릭 → URL 업데이트
   - 인기순 클릭 → 재검색

3. **태그 필터**
   - 태그 클릭 (포함) → 녹색 하이라이트
   - 제외 버튼 (−) 클릭 → 빨간색 하이라이트
   - 필터 요약 표시 확인

4. **필터 초기화**
   - "초기화" 버튼 → 모든 태그 선택 해제

---

## 성능 고려사항

### 현재 구현 (v0.6)

**ILIKE 패턴 매칭:**
- 장점: 간단하고 빠른 구현
- 단점: 대용량 데이터에서 성능 저하

**N+1 쿼리 방지:**
```python
query = select(Video).options(
    selectinload(Video.uploader),
    selectinload(Video.tags)
)
```
- 3개의 쿼리로 모든 데이터 로드 (비디오 + 모든 업로더 + 모든 태그)

---

## 향후 개선사항

### Phase 2 업그레이드 계획

1. **PostgreSQL Full-Text Search**
   - GIN 인덱스 추가
   - `to_tsvector`, `plainto_tsquery` 사용
   - 관련도 순 정렬 (`ts_rank`)

```sql
-- 인덱스 생성
CREATE INDEX idx_videos_title_fts
ON videos USING GIN(to_tsvector('simple', title));

-- 검색 쿼리
SELECT * FROM videos
WHERE to_tsvector('simple', title) @@ plainto_tsquery('simple', 'search term')
ORDER BY ts_rank(to_tsvector('simple', title), plainto_tsquery('simple', 'search term')) DESC;
```

2. **자동완성 (Autocomplete)**
   - 입력 중 추천 검색어 표시
   - 인기 검색어 저장

3. **검색 기록**
   - 사용자별 검색 기록
   - 최근 검색어 표시

4. **고급 필터**
   - 업로드 날짜 범위
   - 조회수 범위
   - 재생 시간 범위

5. **무한 스크롤**
   - Intersection Observer 활용
   - 자동 페이지 로드

---

## 파일 변경 이력

### 백엔드
- ✅ `backend/app/services/video_service.py` - search 메서드 추가
- ✅ `backend/app/api/v1/videos.py` - /search 엔드포인트 추가

### 프론트엔드
- ✅ `frontend/src/components/search/SearchBar.tsx` - 새 파일 생성
- ✅ `frontend/src/pages/SearchPage.tsx` - 새 파일 생성
- ✅ `frontend/src/components/common/Layout.tsx` - SearchBar 통합
- ✅ `frontend/src/App.tsx` - /search 라우트 추가

---

## 참고 자료

- [06_개발_로드맵.md](../06_개발_로드맵.md) - Phase 1 검색 페이지 명세
- [v0.5-tag-system.md](./6.v0.5-tag-system.md) - 태그 시스템 (선행 작업)
- [React Router useSearchParams](https://reactrouter.com/en/main/hooks/use-search-params)
- [PostgreSQL ILIKE](https://www.postgresql.org/docs/current/functions-matching.html)

---

## 변경 이력

| 날짜 | 버전 | 변경 내용 |
|-----|------|----------|
| 2026-01-12 | v0.6.0 | Phase 1 검색 시스템 완료 (ILIKE, include_tags, exclude_tags) |
